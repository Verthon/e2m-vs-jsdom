# Skill: Routing & Navigation

## 1. Route Registration
- **Trigger:** Adding a new Page to the app.
- **Steps:** 1. Add constant to `routesConfig.ts`. 2. Add `React.lazy` and `<Route>` to `Routes.tsx`.
- **Blueprint Placeholder:**
[PASTE YOUR ROUTESCONFIG.TS AND ROUTES.TSX SAMPLES HERE]